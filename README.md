# VirusVania
VirusVania is a lightweight antivirus scanner built in Java that detects suspicious files using signature-based pattern matching. It scans files and directories, supports recursive folder scanning, and uses a customizable signature database. Designed as an educational cybersecurity project, it demonstrates basic malware detection techniques
